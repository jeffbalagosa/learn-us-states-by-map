## Tasks
- [ ] 1. Swap the CRA dependencies for Vite/Vitest tooling (package.json, lockfile) and configure tsconfig for Vite.
- [ ] 2. Add the Vite entry point + config (index.html, main.tsx, vite.config.ts) and remove CRA-only files.
- [ ] 3. Update README to describe the new commands/output and delete the unused deploy.sh GitHub Pages script.
- [ ] 4. Run npm install, npm run build, and npm run test (Vitest) to verify the migration.
